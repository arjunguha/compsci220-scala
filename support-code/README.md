Support code for CMPSCI220
==========================

Development Environment
-----------------------

Use the Vagrant environment in `/vagrant`.

Building
--------

Use `sbt assembly`, not `sbt build`, to package the scala220 and submit220
scripts into .jar files.

