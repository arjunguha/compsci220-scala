name = "clock"
step = "final"

submit += "src/test/scala/*.scala"
submit += "src/main/scala/Clock.scala"

// The command should exit with zero if checks passed and non-zero
// otherwise.
command = ["./test"]
image = "arjunguha/scala"

time-limit = 10s
memory-limit = 200MB